Run TypeScript typechecks on the frontend and report any errors:

```bash
cd frontend && npx tsc --noEmit
```

If there are errors, show the file, line, and error message. Do not attempt to fix errors unless asked.
