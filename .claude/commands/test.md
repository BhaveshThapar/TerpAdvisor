Run the backend test suite and report any failures:

```bash
cd backend && python -m pytest tests/ -v --tb=short
```

If tests fail, show the failing test names and error messages. Do not attempt to fix failures unless asked.
